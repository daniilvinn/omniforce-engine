#ifndef SW_RASTER_QUEUE_GLSLH
#define SW_RASTER_QUEUE_GLSLH

#extension GL_EXT_buffer_reference2         : require
#extension GL_EXT_scalar_block_layout       : require

layout(buffer_reference, scalar, buffer_reference_align = 4) buffer SWRasterQueue {
    uvec3 cluster_count; // uvec3 because it is further used for an indirect compute dispatch
    uint cluster_ids[];
};

#endif