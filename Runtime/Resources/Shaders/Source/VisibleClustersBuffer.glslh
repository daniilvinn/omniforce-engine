#ifndef VISIBLE_CLUSTERS_BUFFER_GLSLH
#define VISIBLE_CLUSTERS_BUFFER_GLSLH

struct VisibleCluster {
	uint instance_index;
	uint cluster_index;
};

layout(buffer_reference, scalar, buffer_reference_align = 4) buffer VisibleClusters {
	uint num_clusters;
	VisibleCluster clusters[];
};

#endif